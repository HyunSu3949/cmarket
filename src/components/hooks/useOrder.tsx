import React from "react";

type Props = {
  order_kind: string;
  product_id: number;
  quantity: number;
};

export default function useOrder({ order_kind }: Props) {
  /* const 
  let orderState = {};
  if (order_kind === "order_cart") {
    orderState = {
      order_kind,
    };
  } else {
  }
  return { orderState }; */
}
